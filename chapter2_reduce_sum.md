## 第二章 从 Reduce Sum 开始理解归约算子

### 第一节 归约计算的物理本质：树状归约与片上数据折叠

上一章围绕逐元素算子，例如 Add，构建了 AIV（Vector Core）上的数据流水线。逐元素算子的数据流是点对点的：输入多少个元素，输出就有多少个元素；每个计算通道互不干扰，数据在 Tile 之间高度独立。

深度学习中还存在另一类完全不同的算子：归约（Reduce）算子。Softmax、LayerNorm、Loss 计算，以及矩阵乘法中的中间规约都会使用它。Reduce Sum 将一个长向量收敛为一个标量，是典型的数据折叠：

$$
y = \sum_{i=0}^{N-1} x_i
$$

从逐元素计算跨越到 Reduce Sum，算子的底层设计逻辑发生了根本变化：数据不再沿着一一对应的通道流向输出，而是在片上逐步汇聚为一个结果。

#### 一、数据折叠的本质与三大工程问题

将 $N$ 个输入元素沿指定维度累加，最终压缩为一个标量或低维张量。这种多对一的计算会带来三个必须解决的问题。

##### （一）输入与输出形状不再对等

逐元素算子中，输入 Tile 切多大，输出 Tile 通常也切多大。Reduce Sum 则不同：即使一个输入 Tile 有 `8192` 个 `float32` 元素，对应的输出也只有一个数值。

这使得内存分配、队列管理和流水线重叠无法再直接套用一对一的模板。算子需要显式管理“完整输入 Tile”如何收缩为“局部和”，并继续参与后续汇聚。

##### （二）逐个累加会浪费向量硬件

在 CPU 上，归约常写成一个串行 `for` 循环：每次从数组取一个数，加到累加器上。但在 AIV 的 Vector 单元上，这种方式会浪费 SIMD 并行能力：

- **硬件算力浪费**：Vector 单元拥有大量并行计算通道；如果每次只累加一个标量，大部分通道都会闲置。
- **流水线数据依赖**：下一次加法依赖上一次加法的结果，连续的标量累加会让向量流水线频繁等待。

归约需要让多组数据在同一时刻参与运算，并用树状结构逐层收缩，才能将 Vector 单元真正利用起来。

##### （三）256 字节对齐是归约输出的硬件约束

昇腾 AI Core 的 Vector 数据读写和指令向量化执行以 `256 B` Block 为基本粒度。对于 `half`（FP16，每个元素 `2 B`），一个 `256 B` Block 包含 `128` 个元素；对于 `float`（FP32，每个元素 `4 B`），一个 Block 包含 `64` 个元素。

```text
昇腾 Vector 寄存器 / UB 的 256 B 数据 Block

┌───┬───┬───┬───┬───┬───────────────────────────┬─────┐
│ 0 │ 1 │ 2 │ 3 │ 4 │             ...           │ 127 │  FP16 示例
└───┴───┴───┴───┴───┴───────────────────────────┴─────┘
└──────────────────────── 256 Bytes ───────────────────────┘
```

Reduce Sum 的最终结果可能只是一个 `float32` 标量，即 `4 B`。这个标量不能脱离对齐约束单独作为一次向量化读写的完整数据块；需要为结果和规约临时空间规划满足硬件要求的 Buffer，并在写回前补足相应的对齐空间。

#### 二、Vector 单元的物理归约过程：树状归约

树状归约（Tree-based Reduction）利用空间并行来缩短数据汇聚的时间。它在 UB 内将大量元素分为三个阶段逐步折叠为一个标量。

```text
阶段 1：向量化纵向折叠（SIMD Fold）

数据块 0 : [ x0,   x1,   x2,  ...  x127 ] ──┐
数据块 1 : [ x128, x129, x130, ...  x255 ] ──┼─> 向量并行加法
...       : [ ...                         ] ──┘
                              │
                              v
              [ S0, S1, S2, ... S127 ]

阶段 2：256 B Block 内部树状归约（Tree Reduction）

              [ S0, S1, S2, ... S127 ]
                │     │           │
                └──┬──┘     └──┬──┘  第 1 阶：折半相加，剩余 64 个
                   [ T0, ... T63 ]
                          │
                         ...       对半折叠直至收敛
                          │
                          v
                        [ sum ]

阶段 3：内存 Padding 与对齐写回

              [ sum, 0, 0, ... 0 ]  ──> 凑齐对齐粒度后写回 GM
```

##### （一）阶段 1：向量化纵向折叠

数据搬入 UB 后，Vector 单元先按 `256 B` 将输入划分为多个向量 Block，并使用 SIMD 指令进行纵向平行累加。大量元素会快速压缩为最后一个 `256 B` 宽度的向量 Block。

##### （二）阶段 2：Block 内部树状归约

最后一个向量 Block 还包含多个元素：FP16 示例中为 `128` 个元素，FP32 示例中为 `64` 个元素。Vector 单元继续执行折半相加：

- 将元素划分为前后两半，在一条向量指令内完成对应位置的并行相加。
- 每一轮折叠后，待处理元素数量减半。
- 直到只剩一个标量 `sum`。

以 `128` 个 FP16 元素为例，树状归约只需 $\log_2(128) = 7$ 轮折叠。每一轮都由多个通道并发完成，避免了标量循环中的长依赖链。

##### （三）阶段 3：结果对齐与写回

树状归约得到最终标量后，框架会将结果放在输出 Buffer 的首地址，并为剩余空间预留 Padding，使其满足 MTE3 写回所需的对齐粒度。随后结果才会安全地从 UB 写回 GM。

#### 三、Reduce Sum 的三阶梯题目设计

理解树状归约与对齐约束后，随着数据规模扩大，算子的设计瓶颈会发生两次转移。下面三题每次只增加一个新的核心问题。

```text
[ Easy 版本 ]                 [ Medium 版本 ]                [ Hard 版本 ]
32 KB 数据                    2 MB 数据                      64 MB 数据
┌──────────────────────┐      ┌──────────────────────┐       ┌──────────────────────┐
│ 单核、单个 Tile      │      │ 单核、多 Tile 循环   │       │ 32 个 AI Core 并行  │
│ 一次性装入 UB        │ ──> │ 突破 UB 容量限制     │ ───> │ 突破单核算力瓶颈     │
│ 聚焦：片上规约链路   │      │ 聚焦：单核片上累加   │       │ 聚焦：多核 Atomic 写│
└──────────────────────┘      └──────────────────────┘       └──────────────────────┘
```

##### （一）Easy 版本：单核单 Tile

- **题目数据**：`8192` 个 `float32` 元素，共 `32 KB`；输入 `x` 的形状为 `(8192,)`，每个元素范围为 $[-1.0, 1.0]$；输出 `y` 的形状为 `(1,)`，范围为 $[-8192, 8192]$。
- **任务要求**：使用单个 AI Core 处理完整输入，将 `x` 搬入 UB，在片上执行 `WholeReduceSum` 或 `BlockReduceSum`，再将标量结果写回 GM。
- **学习重点**：数据从 $N$ 到 $1$ 的折叠过程、256 B 对齐与 `tmpBuffer` 临时空间分配。
- **难点隔离**：暂不引入 Tile 循环与多核并发。

##### （二）Medium 版本：单核多 Tile

- **题目数据**：$2^6 \times 8192 = 524288$ 个 `float32` 元素，共 `2 MB`；输入 `x` 的形状为 `(524288,)`，输出 `y` 的形状为 `(1,)`。
- **任务要求**：仍由单个 AI Core 完成归约。输入必须拆分为多个 Tile，通过循环搬运、片上规约与局部累加得到 Local Sum。
- **学习重点**：单核 Tiling 策略、Tile 偏移、`sum_buffer` 临时存储，以及持续累加各 Tile 局部和的过程。
- **难点隔离**：所有中间结果都在同一个 AI Core 内管理，不引入跨 Core 汇总。

##### （三）Hard 版本：多 Core 协同与跨核归约

- **题目数据**：$32 \times 2\text{ MB} = 64\text{ MB}$，即 `16777216` 个 `float32` 元素；输入 `x` 的形状为 `(16777216,)`，输出 `y` 的形状为 `(1,)`。
- **任务要求**：将输入均分给 `32` 个 AI Core。每个 Core 先计算 `core_local_sum`，再通过 `SetAtomicAdd` 将局部和安全地累加到同一个 GM 输出地址。
- **学习重点**：基于 `block_idx` 的跨核数据切分、多核写同一输出地址时的竞争问题，以及硬件 Atomic Add 的使用。
- **延伸问题**：比较 Atomic 规约与 Two-Stage 规约。后者通过 WorkSpace 保存各 Core 局部和，再进行第二次归约，以降低原子写带来的开销。
