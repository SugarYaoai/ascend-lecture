## 第二章 从 Reduce Sum 开始理解归约算子

### 第一节 Reduce Sum Easy：从向量到标量

Reduce Sum 是最基础的归约（Reduction）算子之一。它接收一个一维向量，将其中所有元素累加，最终输出一个标量：

$$
y = \sum_{i=0}^{N-1} x_i
$$

与 Add 的逐元素计算不同，Add 的每个输出位置只依赖一对输入元素；Reduce Sum 的一个输出值则依赖整个输入向量。也就是说，数据不再是一一对应地流向输出，而是从许多元素逐步汇聚为一个结果。

#### 一、Reduce Sum Easy 的题目规格

Easy 版本先使用固定长度的一维 `float32` 向量，重点理解“从多个元素汇聚到一个标量”的计算过程。

| 变量 | 数据类型与形状 | 数值范围 |
| --- | --- | --- |
| 输入 `x` | 一维 `float32` 向量，形状为 `(8192,)` | 每个元素 $x_i \in [-1.0, 1.0]$ |
| 输出 `y` | 单个 `float32` 标量，形状为 `(1,)` | $y \in [-8192, 8192]$ |

因此，本题需要计算：

$$
y = x_0 + x_1 + x_2 + \cdots + x_{8191}
$$

`8192` 个 `float32` 输入元素占用：

$$
8192 \times 4\text{ B} = 32768\text{ B} = 32\text{ KB}
$$

这份输入可以直接放入单个 AI Core 的 UB。Easy 版本暂时只处理一个 Block 内的局部归约：先将整个输入搬入 UB，再由 Vector 单元完成累加，最后将一个结果写回 GM。

#### 二、从 Add 到 Reduce Sum：输出关系发生了什么变化

Add 的输入与输出具有一一对应关系：

$$
z_i = x_i + y_i
$$

而 Reduce Sum 将全部输入元素汇聚到唯一的输出位置：

$$
\left[x_0, x_1, \ldots, x_{8191}\right] \longrightarrow y
$$

这意味着 Reduce Sum 的核心不只是“调用一次加法指令”，而是设计一条安全的汇聚路径：每一批局部结果都要继续参与下一轮累加，直到 UB 中只剩下一个最终结果。后续小节将从这一条归约路径出发，逐步实现第一个 Reduce Sum Kernel。
