## 第二章 从 Reduce Sum 开始理解归约算子

### 第一节 Reduce Sum Easy：从向量到标量

Reduce Sum 是最基础的归约（Reduction）算子之一。它接收一个一维向量，将其中所有元素累加，最终输出一个标量：

$$
y = \sum_{i=0}^{N-1} x_i
$$

#### 一、Reduce Sum 的三阶梯题目设置

三个版本保持相同的归约规则与输入类型：

$$
y = \sum_{i=0}^{N-1} x_i, \quad x_i \in [-1.0, 1.0]
$$

数据规模与并行方式逐级扩展，每一题只增加一个新的核心问题。

##### （一）Easy 版本：单核单 Tile

- **题目数据**：`8192` 个 `float32` 元素，共 `32 KB`。
- **输入与输出**：输入 `x` 的形状为 `(8192,)`；输出 `y` 的形状为 `(1,)`。
- **任务要求**：使用单个 AI Core 处理完整输入，将 `x` 从 GM 搬入 UB，在片上完成 `WholeReduceSum` 或 `BlockReduceSum`，将最终标量写回 GM。
- **学习重点**：单核单 Tile 的归约链路、256 B 对齐，以及规约临时空间 `tmpBuffer` 的分配。
- **难点隔离**：暂不引入 Tile 循环与多核并发。

##### （二）Medium 版本：单核多 Tile

- **题目数据**：$2^6 \times 8192 = 524288$ 个 `float32` 元素，共 `2 MB`。
- **输入与输出**：输入 `x` 的形状为 `(524288,)`；输出 `y` 的形状为 `(1,)`。
- **任务要求**：仍由单个 AI Core 完成归约。输入无法一次装入 UB，需要拆为多个 Tile，通过循环搬运、片上规约与局部累加得到一个 Local Sum。
- **学习重点**：单核 Tiling 策略、Tile 偏移、`sum_buffer` 临时存储，以及在 Tile 循环中持续累加中间结果。
- **难点隔离**：不引入跨 Core 汇总；所有中间归约结果仍由当前 AI Core 管理。

##### （三）Hard 版本：多 Core 协同与跨核归约

- **题目数据**：$32 \times 2\text{ MB} = 64\text{ MB}$，即 `16777216` 个 `float32` 元素。
- **输入与输出**：输入 `x` 的形状为 `(16777216,)`；输出 `y` 的形状为 `(1,)`。
- **任务要求**：将输入均分给 `32` 个 AI Core。每个 Core 先计算自己的 `core_local_sum`，再使用 `SetAtomicAdd` 将局部和安全地累加到同一个 GM 输出地址。
- **学习重点**：基于 `block_idx` 的跨核数据切分、多核写同一输出地址时的竞争问题，以及硬件 Atomic Add 的使用。
- **延伸问题**：比较 Atomic 规约与 Two-Stage 规约：前者代码直接，后者通过 WorkSpace 保存各 Core 的局部和，再进行第二次归约以降低原子写带来的开销。
