# 动手学昇腾算子开发

## 第一章 从 Add 开始理解昇腾算子开发

### 第一节 认识昇腾算子开发

#### 一、从一次向量加法说起

先看一个最熟悉的计算：给定两个等长数字序列 `x`、`y`，生成一个新序列 `z`；每个位置只做一次加法：

$$
z_i = x_i + y_i
$$

在深度学习和张量计算中，像这样“接收若干输入数据，按固定规则产生输出数据”的计算单元，称为**算子（Operator）**。这里的 Add 就是最简单的算子之一。昇腾算子开发要解决的问题，是怎样让 NPU 高效完成这条计算规则。

在深度学习中，像 Add 这样“接收输入、按照固定规则产生输出”的计算单元称为**算子（Operator）**。为了让 NPU 执行 Add，需要编写运行在设备侧的 **Kernel（核函数）**；Kernel 中的代码负责把这条相加规则真正落实为设备上的计算过程。

#### 二、NPU 算子为什么能比普通 CPU 程序更快

对规则固定、数据量足够大的张量计算，NPU 可以比按顺序处理数据的普通 CPU 程序更快。最直接的原因是 NPU 配备了面向张量计算的专用硬件，并能同时处理大量彼此独立的数据。

**1. 向量指令批量计算**

Add 不必写成“取一个数、加一次、再取下一个数”的标量循环。AI Core 的向量计算单元可以用一条向量指令处理一批连续元素，从而获得很高的计算吞吐量。

**2. 多个专用执行单元可以重叠工作**

AI Core 内不只有一种“执行加法”的指令：从设备内存读取数据、执行向量计算、把结果写回内存，分别由不同的专用执行单元处理。它们可以同时推进不同数据块的工作，因此代码按顺序发出命令，并不意味着每条命令都要完成后下一条才开始。正确处理这些异步指令之间的依赖关系，才能把读、算、写的等待时间隐藏起来。

#### 三、初学算子开发时的两个主要难点

NPU 的硬件能力带来速度，也要求开发者理解它的数据路径和执行方式：

1. **多层级的内存管理。** 输入先在 CPU 一侧准备，再进入 NPU 的设备内存；真正计算时，还要把当前需要的一小段数据搬进 AI Core 内部更快的片上存储。代码需要明确处理“完整数据放在哪里、当前数据放在哪里、何时搬入和写回”。
2. **复杂的并行。** 完整输入需要划分给多个核心同时处理；而在同一个核心内部，读数据、执行向量计算、写回结果也可由不同执行单元推进。前者是跨核心并行，后者是核内并行；后文会用 Block 划分、Tile 分块和双缓冲逐步展开。

#### 四、本章的学习路线

后续各节每次只引入一个新问题：

1. **第二节到第四节：先把 Add 算对。** 依次认识数据流、直接启动 Kernel，以及 C API 与 C++ API 两种表达方式。
2. **第五节：让数据放得下。** 输入变长后，一个核心无法一次容纳全部工作数据，需要在内部继续分块。
3. **第六节：让硬件少等待。** 在结果正确、数据能够放入片上存储后，再让相邻数据块的读、算、写尽可能重叠。
