# 动手学昇腾算子开发

## 第一章 从 Add 开始理解昇腾算子开发

### 第一节 认识昇腾算子开发

#### 一、从一次向量加法说起

先看一个最熟悉的计算：给定两个等长数字序列 `x`、`y`，生成一个新序列 `z`；每个位置只做一次加法：

$$
z_i = x_i + y_i
$$

在深度学习和张量计算中，像这样“接收若干输入数据，按固定规则产生输出数据”的计算单元，称为**算子（Operator）**。这里的 Add 就是最简单的算子之一。昇腾算子开发要解决的问题，是怎样让 NPU 高效完成这条计算规则。

这条规则本身只说明“要算什么”，还没有说明“由谁执行”。本书选择让 NPU 执行 Add；运行在 NPU 上、负责落实这条规则的设备侧程序，称为 **Kernel（核函数）**。

这里先记住三个足以继续阅读的词：

- **张量（Tensor）**：按形状组织的一组数。本书先从一维张量，即向量，开始学习。
- **算子（Operator）**：一条张量计算规则及其实现；Add 的规则就是对应位置相加。
- **Kernel（核函数）**：运行在 NPU 设备侧、实际执行算子计算的程序。

#### 二、为什么从 Add 开始

Add 的数学规则足够简单，读者可以把注意力放在程序如何运行，而不是公式本身。即使只是逐元素相加，也已经包含了算子开发中反复出现的基本问题：

- Host 如何启动 NPU Kernel；
- 多个 Block 如何划分完整输入；
- 数据为什么要在 GM 与 UB 之间搬运；
- UB 放不下整段数据时，如何分块；
- 数据搬运与向量计算如何进一步重叠。

这些问题会在更复杂的矩阵乘法、归约、注意力计算等算子中再次出现，只是数据布局和计算规则会更复杂。

#### 三、由浅入深的五个阶段

后续各节围绕同一个 Add 算子逐步增加一个新的问题，而不是同时引入所有概念：

1. **编写第一个 Add Kernel**：认识 Host、GM、UB、AI Core、Block，并完成一次直接启动的 Add。
2. **用 C API 实现 Add**：在 TensorOJ 模板中填写 `kernel.asc`，使用 SIMD C API 完成同一计算。
3. **用 C++ API 实现 Add**：使用 `GlobalTensor`、`LocalTensor` 等 C++ API 表达相同的数据搬运与向量计算。
4. **用 Tile 处理更长的向量**：扩大输入长度；当一个 Block 的数据无法一次放入 UB 时，在 Block 内按 Tile 循环处理。
5. **用双缓冲减少等待**：让相邻 Tile 的搬运、计算和写回形成重叠，减少执行单元等待。

#### 四、两种层级的并行与分块

`Block` 和 `Tile` 名字相近，但回答的是不同问题：

```text
Block：把完整输入划分为多个独立任务，交给多个 AI Core 调度执行。
Tile ：把一个 Block 内的数据分成多次放入 UB 的小片段。
```

后续代码中，`block_idx` 用于定位当前 Block 负责的数据范围；Tile 循环则在这一范围内反复复用 UB 缓冲区。这两个层级共同决定了数据怎样在 NPU 上流动。
