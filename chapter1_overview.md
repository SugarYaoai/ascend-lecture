# 动手学昇腾算子开发

## 第一章 从 Add 开始理解昇腾算子开发

### 第一节 认识昇腾算子开发

#### 一、从一次向量加法说起

先看一个最熟悉的计算：给定两个等长数字序列 `x`、`y`，生成一个新序列 `z`；每个位置只做一次加法：

$$
z_i = x_i + y_i
$$

在深度学习中，像 Add 这样“接收输入、按照固定规则产生输出”的计算单元称为**算子（Operator）**。为了让 NPU 执行 Add，需要编写运行在设备侧的 **Kernel（核函数）**；Kernel 中的代码负责把这条相加规则真正落实为设备上的计算过程。

#### 二、大模型时代下的 NPU 与大规模并行计算

从 Transformer 架构的普及到大语言模型的发展，深度学习的计算规模不断扩大。训练和推理的主体不再是少量标量运算，而是海量张量上的矩阵、向量与归约计算；它们需要极高的浮点吞吐量，也持续向设备内存带宽施压。

即使开启多线程，通用 CPU 的设计重点仍是低延迟控制、分支处理与通用任务调度，难以独自提供大规模张量计算所需的数据并行吞吐。面向这类工作负载，昇腾 AI 加速卡提供了专用的 NPU（Neural Processing Unit，神经网络处理器）。日常也常把它称为“昇腾显卡”，但它的主要定位是 AI 计算加速，而非传统图形渲染。

##### （一）CPU 负责调度，NPU 负责并行计算

在 AI 服务器中，CPU 和 NPU 是协作关系，而不是替代关系：CPU 保持对程序流程的控制，NPU 接管规则固定、数据并行度很高的数值计算。

```text
                 ┌─────────────────────────────────────┐
                 │           CPU：控制与调度            │
                 │  业务逻辑、分支判断、数据准备、下发任务 │
                 └──────────────────┬──────────────────┘
                                    │
                                    │ 提交并行计算任务
                                    ▼
                 ┌─────────────────────────────────────┐
                 │          昇腾 NPU：并行计算          │
                 │  对大批量数据执行相同的数学规则      │
                 └─────────────────────────────────────┘
```

CPU 擅长处理 Token 预处理、控制流分支和任务提交等控制密集型工作；NPU 则擅长矩阵乘法、卷积、归约以及本章的逐元素 Add 等计算密集型工作。让 CPU 管理“下一步做什么”，让 NPU 并行执行“对大量数据做同一种计算”，才能同时利用二者的优势。

##### （二）一个算子如何展开为多级并行

NPU 并不是一个单体计算核心，而是由多个 AI Core 组成的并行计算设备。一个高层 Add 算子提交后，会沿着不同层级展开：

1. **任务提交。** CPU 通过昇腾 Runtime 将 Kernel 启动任务放入 Stream；Stream 是按顺序提交设备任务的执行队列。
2. **Block 级空间并行。** Kernel 启动时指定多个逻辑 Block。每个 Block 负责完整输入的一个独立数据区间，运行时将这些 Block 调度到可用的 AI Core 上执行。
3. **AI Core 内的向量并行。** 每个 AI Core 的 Vector 单元使用 SIMD 指令，在一条指令中处理一批连续元素；因此，一个 Block 内也不是逐个元素进行标量加法。

这三层关系可以概括为：CPU 提交任务，多个 AI Core 并行处理多个 Block，而每个 AI Core 再以向量指令批量处理自己的数据段。

##### （三）算子开发在这条链路中的位置

理解了这条并行链路，就能明确昇腾算子开发的定位：它是在为大模型基础设施编写运行于 NPU AI Core 上的设备侧高并发程序。

开发者不仅要给出 Add、MatMul 等算子的数学规则，还要设计多 Core 如何分工、局部片上存储如何流转，以及搬运与计算如何协调。后续章节中的 Block、UB、Tile 和双缓冲，正是把这条宏观并行链路落实为可执行 Kernel 代码的工具。

#### 三、昇腾 NPU 算子的高性能来源

对规则固定、数据量足够大且具有数据并行性的张量计算，昇腾 NPU 能够充分利用专为 AI 负载设计的硬件。性能并非来自某一条加法指令，而是来自向量计算、多 Core 执行和异步流水线的共同作用。

**1. SIMD 向量计算带来高吞吐量**

Add 不必写成“取一个数、加一次、再取下一个数”的标量循环。AI Core 的 Vector 单元可以用一条 SIMD（Single Instruction, Multiple Data）指令处理一批连续元素，从而在单位时间内完成大量相加操作。

**2. 多 AI Core 带来空间并行**

一个很长的向量可以被划分为多个连续数据段。每一段对应一个 Block，即同一份 Kernel 的一个逻辑执行任务；多个 AI Core 可以同时执行这些 Block。每个 Core 负责不同的数据区域，整个 NPU 因此能够同时推进许多相互独立的计算任务。

**3. 搬运与计算解耦的异步流水线**

AI Core 内部既有负责数据搬运的 MTE（Memory Transfer Engine），也有负责向量计算的 Vector 单元。MTE 可以在 Global Memory（GM）与 Unified Buffer（UB）之间搬运数据，Vector 单元则在 UB 中执行计算。不同单元能够处理不同数据块的工作，因此读数据、计算、写回可以形成异步流水线；源码按顺序发出命令，并不意味着硬件必须按顺序完成所有命令。

#### 四、初学昇腾算子开发的三类挑战

这些硬件能力带来速度，也要求开发者在代码中显式处理以下关系：

1. **显式内存层级管理。** GM 是设备侧的大容量共享内存，适合保存完整输入输出；UB 是 AI Core 内部容量较小、速度更高的片上 Local Memory，适合保存当前计算的数据段。算子必须明确控制数据何时从 GM 搬入 UB、何时写回 GM，而不能把两者当成同一种内存。
2. **多 Core 任务切分与数据分块。** 同一份 Kernel 需要将完整输入划分为多个 Block，交给多个 AI Core 执行。当一个 Block 的数据仍然超过 UB 容量时，还要继续切成多个 Tile，即可放入 UB 的连续小数据段，分批处理。这里既要保证不同任务覆盖完整数据且不重复，也要让每一小段数据符合片上存储限制。
3. **核内流水线编排与同步控制。** MTE 搬运和 Vector 计算可以异步推进，但数据没有搬入完成就不能开始计算，结果没有计算完成也不能写回。要让读、算、写安全重叠，程序需要表达这些依赖关系；后文将用同步操作和双缓冲逐步解决这一问题。

这三类挑战分别对应内存管理、任务划分和并行控制，也是昇腾算子代码比普通顺序程序更需要仔细设计的原因。

#### 五、本章的学习路线

本章将以 Add 为例，由浅入深地讲解上述硬件性能来源，以及实现它们时的三类开发挑战：

1. **第二节：建立最简数据路径。** 认识 GM、UB 与 Vector 单元的分工，完成“GM 搬入 UB、向量加法、写回 GM”的最简 Kernel 闭环。
2. **第三节和第四节：掌握 Ascend C 的两种表达方式。** 在相同 Add 路径上，分别使用指针风格的 SIMD C API 与张量对象风格的 C++ API，表达地址、局部缓冲区、搬运和计算。
3. **第五节：解决片上存储容量限制。** 当一个 Block 的数据无法放入 UB 时，引入 Tile，将大任务拆成多次搬入和计算的小段。
4. **第六节：利用异步流水线减少等待。** 通过同步关系和双缓冲，让不同 Tile 的搬运、计算和写回尽可能重叠。
